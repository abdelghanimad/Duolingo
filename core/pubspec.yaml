name: global_guess_live_core
description: Pure-Dart domain layer (models, utils, win-detection logic) shared by the Flutter app.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.4.0 <4.0.0"

dev_dependencies:
  test: ^1.25.0
  lints: ^4.0.0
